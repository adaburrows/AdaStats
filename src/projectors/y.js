/**
 * y() returns the y coordinate from a point
 *
 * @param {Array} data - array of data points
 * @param {number} i - index for the i^th point
 * @returns {number} - the y coordinate from the i^th point
 */
function y (data, i) {
  return data[i][1];
}

export default y;
