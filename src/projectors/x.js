/**
 * x() returns the x coordinate from a point
 *
 * @param {Array} data Array of data points
 * @param {number} i Index for the i^th point
 * @returns {number} The x coordinate from the i^th point
 */
function x (data, i) {
  return data[i][0];
}

export default x;
