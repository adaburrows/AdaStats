/**
 * integer_generator() returns an initial zero for a bin to accumulate values
 *
 * @returns {number} - integer accumulator
 */
function integer_generator () {
  return 0;
}

export default integer_generator;
